{
    "errcode":${errcode},
    "errmessage":"${errmessage}",
    "data":{
        "lawyerRelate":{
                "fansCount":"${(data.fansCount)!0}",
                "issueCount":"${(data.issueCount)!0}",
                "interestCountMy":"${(data.interestCountMy)!0}"
    }
    }
}
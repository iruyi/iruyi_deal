{
    "errcode":${errcode},
    "errmessage":"${errmessage}",
    [#if data?? && data?size>0]
        "data":[
            [#list data as a]
                {
                  "id":${a.id},
                  "createDate":"${a.createDate?datetime}",
                  "officeName":"${a.officeName}",
                  "type":"${a.type}",
                  "cityName":"${a.cityName}",
                  "salary":"${a.salary!0}",
                  "photo":"${a.photo!!}"
                }
                [#if a_has_next],[/#if]
            [/#list]
               ]
    [#else]
        "data":[]
    [/#if]
}


{
    "errcode":${errcode},
    "errmessage":"${errmessage}",
    "data":{
        [#if data?? && data?size>0]
            "positionList":[
                [#list data as a]
                    {
                      "id":${a.id},
                      "createDate":"${a.createDate?datetime}",
                      "officeName":"${a.officeName!!}",
                      "type":${a.type!0},
                      "cityName":"${a.cityName}",
                      "salary":"${a.salary!0}",
                      "photo":"${a.photo!!}"
                    }
                    [#if a_has_next],[/#if]
                [/#list]
                   ]
        [#else]
            "positionList":[]
        [/#if]
    }
}


{
    "errcode":${errcode},
    "errmessage":"${errmessage}",
    "data":{
        [#if data?? && data?size>0]
            "topicReplyList":[
                [#list data as a]
                    {
                      "lawyerId":${lawyer.id},
                      "lawyerName":"${lawyer.name!!}",
                      "photoUrl":"${lawyer.photoUrl!!}",
                      "replyiId":${a.id},
                      "replyContent":"${a.content}",
                      "createDate":"${a.createDate?datetime}",
                      "topicId":${a.topicVo.id},
                      "topicContent":"${a.topicVo.content}"
                    }
                    [#if a_has_next],[/#if]
                [/#list]
                   ]
        [#else]
            "topicReplyList":[]
        [/#if]
    }
}
{
    "errcode":${errcode},
    "errmessage":"${errmessage}",
    "data":{
        [#if data?? && data?size>0]
            "lawyerList":[
                [#list data as a]
                    {
                      "id":${a.id},
                      "name":"${a.name!!}",
                      "photoUrl":"${a.photoUrl!!}"
                    }
                    [#if a_has_next],[/#if]
                [/#list]
                   ]
        [#else]
            "lawyerList":[]
        [/#if]
    }
}
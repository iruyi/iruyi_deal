{
    "errcode":${errcode},
    "errmessage":"${errmessage}",
    [#if data?? && data?size>0]
        "data":{"articleList":[
            [#list data as a]
                {
                  "articleId":${a.id},
                  "type":${a.type},
                  "comment":"${a.comment}",
                  "url":"${a.url}",
                  "acro":"${a.acro}",
                  "commentCount":${a.commentCount},
                  "praiseCount":${a.praiseCount},
                  "isPraise":${a.isPraise}
                }
                [#if a_has_next],[/#if]
            [/#list]
               ]}
    [#else]
        "data":{"articleList":[]}
    [/#if]
}